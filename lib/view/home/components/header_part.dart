import 'package:flutter/material.dart';

class HeaderPart extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('HeaderPart')),
    );
  }
}
