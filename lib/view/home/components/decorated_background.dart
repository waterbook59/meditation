import 'package:flutter/material.dart';

class DecoratedBackground extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('DecoratedBackground')),
    );
  }
}
