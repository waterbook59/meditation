import 'package:flutter/material.dart';

class VolumeSliderPart extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('VolumeSliderPart')),
    );
  }
}
