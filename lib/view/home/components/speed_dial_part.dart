import 'package:flutter/material.dart';

class SpeedDialPart extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('SpeedDialPart')),
    );
  }
}
